# App Icon

Put the Windows icon file here:

```text
assets/app.ico
```

Use a square logo and export it as an `.ico` file with common sizes such as 16, 32, 48, 128, and 256 px.

After replacing `app.ico`, run:

```powershell
.\build_exe.bat
```
